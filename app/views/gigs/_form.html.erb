<%= form_for(@gig) do |f| %>
  <% if @gig.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gig.errors.count, "error") %> prohibited this gig from being saved:</h2>

      <ul>
      <% @gig.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :band_id %><br />
    <%= f.collection_select :band_id, Band.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :concert_id %><br />
    <%= f.collection_select :concert_id, Concert.all, :id, :venue %>
  </div>
  <div class="field">
    <%= f.label :compensation %><br />
    <%= f.number_field :compensation %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
